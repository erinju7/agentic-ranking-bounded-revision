# Core runtime for the current dissertation pipeline (Study 1 BRIGHT reranking +
# Study 2 applied matching). Versions are lower bounds, not a lockfile; the runs
# were not pinned to exact releases.
anthropic>=0.40
google-generativeai>=0.8
certifi
scikit-learn>=1.3      # TF-IDF hard-negative mining and the BM25/lexical baseline
numpy>=1.24
scipy>=1.10
pandas>=2.0

# Open-weight backbones are called through the OpenAI-compatible client
# (scripts/oai_client.py), which uses the standard library, not the openai SDK.

# Optional extras, needed only for specific auxiliary scripts:
#   sentence-transformers, torch  -> cross-encoder reference baseline (bright_crossencoder_baseline.py)
#   statsmodels                   -> headroom / pooled cross-model analyses
#   matplotlib                    -> regenerating figures
# Legacy/exploratory scripts (rq2_*, CORDIS, langgraph/fastapi prototypes) are not
# part of the reported pipeline and pull heavier dependencies; install those ad hoc.
